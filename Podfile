platform :ios, '9.0'
use_frameworks!

target 'GithubClient' do
        #Network Layer dependencies
        pod 'Siesta', '~> 1.0'
        pod 'Siesta/UI', '~> 1.0'
        pod 'SwiftyJSON'
        pod 'PagedArray'
end


